using Microsoft.EntityFrameworkCore;

namespace Spriggan.Data.Main;

public static class Seeding
{
    public static void Seed(ModelBuilder builder)
    {
        // TODO: Add seeding configurations here.
    }
}
